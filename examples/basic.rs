use fxkv::db::Db;
use fxkv::Result;

fn main() -> Result<()> {
    // let db = Db::new()?;
    // db.open_tree("tree1")?;
    // db.open_tree("tree2")?;
    // let trees  = db.start_transaction(["tree1","tree2"])?;
    // let t1 = trees.get(0);
    // t1.set("key1","value1".as_bytes().to_vec())?;
    Ok(())
}
